import React from 'react';

/**
 * Should have two buttons, one for joining a game with music (must be 
 * logged in) and one for joining a game without music (can be logged in or not) 
 * This option should warn them that the music will not play on their device and 
 * should be close enough to hear the host's device. When logging in as a guest,
 * the user should be prompted to enter a username.
 */

const JoinGame = () => {
  return <div>Join Game Page</div>;
};

export default JoinGame;